@using BSSiseveeb.Public.Web.Models
@using BSSiseveeb.Core.Domain

@model BaseViewModel

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - Brightspark Siseveeb</title>
    @Styles.Render("~/Content/css")
    @Styles.Render("~/Content/datepicker.css")
    @Scripts.Render("~/bundles/modernizr")

</head>
<body>
    <div class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>{ 
                @Html.ActionLink("Brightspark", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
            </div>
            <div class="navbar-collapse collapse">
                <ul class="nav navbar-nav">
                    @if (Request.IsAuthenticated)
                    {
                        <li>@Html.ActionLink("Puhkuse Kalender", "Calendar", "Home")</li>
                        <li>@Html.ActionLink("Töötajad", "Workers", "Home")</li>
                        <li>@Html.ActionLink("Töömaterjalid", "Requests", "Home")</li>
                        if (Model.CurrentUserRole.Rights.HasFlag(AccessRights.Users) || Model.CurrentUserRole.Rights.HasFlag(AccessRights.Vacations) || Model.CurrentUserRole.Rights.HasFlag(AccessRights.Requests))
                        {
                            <li class="dropdown">
                                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Admin <span class="caret"></span></a>
                                <ul class="dropdown-menu">
                                    @if (Model.CurrentUserRole.Rights.HasFlag(AccessRights.Vacations))
                                    {
                                        <li>@Html.ActionLink("Puhkused", "Vacations", "Admin")</li>
                                    }
                                    @if (Model.CurrentUserRole.Rights.HasFlag(AccessRights.Requests))
                                    {
                                        <li>@Html.ActionLink("Töömaterjalide taotlused", "Requests", "Admin")</li>
                                    }
                                    @if (Model.CurrentUserRole.Rights.HasFlag(AccessRights.Users))
                                    {
                                        <li>@Html.ActionLink("Sünkroniseeri kasutajad", "SyncUsers", "Admin")</li>
                                        <li>@Html.ActionLink("Töötajad", "EditEmployees", "Admin")</li>
                                    }
                                </ul>
                            </li>
                        }
                    }
                </ul>
                @Html.Partial("_LoginPartial")
            </div>
        </div>
    </div>
    <div class="container body-content">
        @RenderBody()
        <hr />
        <footer>
            <p>&copy; Brightspark 2016</p>
        </footer>
    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</body>
</html>
